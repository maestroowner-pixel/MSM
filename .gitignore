node_modules/
.expo/
dist/
web-build/
# Web build output. Produced by ../MSM Win Web and exported INTO this repo because
# `firebase deploy` refuses a hosting public path outside the project directory.
web-dist/
*.log
*.swp
.DS_Store
ios/
android/
# react-native-windows project is generated on the Windows dev machine (see WINDOWS.md)
windows/
*.binlog
.env*.local

# Android signing — NEVER commit the upload keystore or its passwords
credentials/
*.jks
*.keystore
!android/app/debug.keystore
keystore.properties

# JetBrains IDE
.idea/

# Firebase CLI deploy cache — regenerated on every deploy
.firebase/
# Cloud Functions dependencies (installed with `cd functions && npm i`)
functions/node_modules/
